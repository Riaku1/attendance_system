<?php
// This script and data application were generated by AppGini 23.14
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/emp_id.php');
	include_once(__DIR__ . '/emp_id_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('emp_id');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'emp_id';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`emp_id`.`id`" => "id",
		"`emp_id`.`field2`" => "field2",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`emp_id`.`id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`emp_id`.`id`" => "id",
		"`emp_id`.`field2`" => "field2",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`emp_id`.`id`" => "ID",
		"`emp_id`.`field2`" => "Field 2",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`emp_id`.`id`" => "id",
		"`emp_id`.`field2`" => "field2",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`emp_id` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'emp_id_view.php';
	$x->TableTitle = 'Attendance';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`emp_id`.`id`';

	$x->ColWidth = [150, ];
	$x->ColCaption = ['Field 2', ];
	$x->ColFieldName = ['field2', ];
	$x->ColNumber  = [2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/emp_id_templateTV.html';
	$x->SelectedTemplate = 'templates/emp_id_templateTVS.html';
	$x->TemplateDV = 'templates/emp_id_templateDV.html';
	$x->TemplateDVP = 'templates/emp_id_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: emp_id_init
	$render = true;
	if(function_exists('emp_id_init')) {
		$args = [];
		$render = emp_id_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: emp_id_header
	$headerCode = '';
	if(function_exists('emp_id_header')) {
		$args = [];
		$headerCode = emp_id_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: emp_id_footer
	$footerCode = '';
	if(function_exists('emp_id_footer')) {
		$args = [];
		$footerCode = emp_id_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
